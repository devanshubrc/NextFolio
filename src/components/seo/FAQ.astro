---
/**
 * FAQ.astro — Accordion FAQ section with FAQPage JSON-LD schema.
 *
 * Renders expandable FAQ items using native `<details>` / `<summary>` elements
 * and injects FAQPage structured data for SEO.
 *
 * @example
 * ```astro
 * <FAQ items={[
 *   { question: "Is it free?", answer: "Yes, completely free." },
 *   { question: "Do I need to sign up?", answer: "No account required." },
 * ]} />
 * ```
 */

interface Props {
  /** Array of FAQ question/answer pairs */
  items: { question: string; answer: string }[];
  /** Section heading — defaults to 'Frequently Asked Questions' */
  title?: string;
}

const { items, title = 'Frequently Asked Questions' } = Astro.props;

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: items.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.answer,
    },
  })),
};
---

<section class="w-full max-w-3xl mx-auto py-16 px-4" id="faq">
  <h2 class="text-3xl font-bold text-white text-center mb-10">
    {title}
  </h2>

  <div class="space-y-3">
    {items.map((item, index) => (
      <details
        class="group bg-slate-800/50 backdrop-blur-sm border border-slate-700/50 rounded-2xl overflow-hidden transition-all duration-300 hover:border-slate-600/50"
      >
        <summary class="flex items-center justify-between cursor-pointer px-6 py-5 text-white font-medium text-lg select-none list-none [&::-webkit-details-marker]:hidden">
          <span class="pr-4">{item.question}</span>
          <span class="shrink-0 text-slate-400 transition-transform duration-300 group-open:rotate-45 text-2xl leading-none">
            +
          </span>
        </summary>
        <div class="px-6 pb-5 text-slate-300 leading-relaxed border-t border-slate-700/50 pt-4">
          {item.answer}
        </div>
      </details>
    ))}
  </div>
</section>

<!-- JSON-LD: FAQPage -->
<script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
